# ============================================================
# NUJ India — Development Requirements (Windows / SQLite)
# Use this for local development — no PostgreSQL needed.
# For production, use requirements.txt
# ============================================================

# Core Django
Django==5.0.6
python-dotenv==1.0.1

# Rich text editor
django-ckeditor-5==0.2.13

# HTMX integration
django-htmx==1.19.0

# Static files
whitenoise==6.7.0

# Media/image handling
Pillow==10.4.0

# Tagging
django-taggit==5.0.1

# Extensions (shell_plus, etc.)
django-extensions==3.2.3

# Slug utility
python-slugify==8.0.4

# Testing
pytest==8.2.2
pytest-django==4.8.0

# Production server (needed even in dev for runserver)
gunicorn==22.0.0

# NOTE: psycopg2-binary is intentionally excluded.
# SQLite is used in development. Add psycopg2-binary for production.
